#include <stdio.h>
#include <string.h>

typedef struct Students{
    char name[5];
    char branch[5];
    int ID_no;
} stu;

int main(int argc, char const *argv[])
{
    stu s;
    strcpy()
    printf("%s\n", s.name);
    printf("%s\n",s.branch);
    printf("%c\n", s.branch[200]);
    return 0;
}