#include <bits/stdc++.h>
using namespace std;

class Solution
{
public:
    int numRookCaptures(vector<vector<char>> &board)
    {
        vector<vector<char>> b = board;
        int n = b.size();
        for (int i = 0; i < n; i++)
        {
            for (int j = 0; j < n; j++)
            {
                if (board[i][j] == 'B')
                {
                    for (int i = 0; i < count; i++)
                    {
                        /* code */
                    }
                }
            }
        }
    }
};