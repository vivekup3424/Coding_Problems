/**Give array of quantity of production quantity[i] and power consumed by machine i 
 * i.e. power[i]. 
 * Find the maximum quantity of production such that total power 
 * consumed does not exceed MAX_POWER.
*/

#include <bits/stdc++.h>
using namespace std;
long maxQuantity(int[] power, int[] quantity, int maxPower){
    int n = sizeof(power)/sizeof(int);
    int dp

}
