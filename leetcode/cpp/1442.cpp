#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    int countTriplets(vector<int>& arr) {
        int score = 0;
        int n = arr.size();
        for (int i = 0; i < n; i++) {
            
        }
        return score;
    }
};
