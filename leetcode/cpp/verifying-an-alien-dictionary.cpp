#include <bits/stdc++.h>
using namespace std;

class Solution{
private:
    unordered_map<char,int> indices;
    bool checkOrder(string &a, string &b){
        for(int i = min(a.length(),b.))
    }
public:

}
