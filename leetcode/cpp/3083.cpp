#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    bool isSubstringPresent(string s) {
        set<string> hashset;
        int n = s.size();
        for(int i = 1; i < n;i ++){
            
        }
    }
};