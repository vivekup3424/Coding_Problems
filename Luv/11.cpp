// Why Print (answer) modulo 10^9+7 |
// Modular Arithmeric |
// EP 11`

//  (a + b) % M = ((a % M) + (b % M)) % M
//  (a * b) % M = ((a % M) * (b % M)) % M
//  (a - b) % M = ((a % M) - (b % M)+M) % M
//  (a / b) % M = ((a % M) * (b^-1 % M)) % M
// b^-1 = multiplicative inverse of b

/*Give a number N, print its factorial
Constriaints
1 <= N <= 100*/

#include <bits/stdc++.h>
using namespace std;
typedef long long int lli;
typedef long long unsigned llu;
inline void fast_io()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
}
int main()
{
    fast_io();
    int T;
    cin >> T;
    while (T--)
    {
        set<int> st;
        map<int,int> fq;
    }
}
