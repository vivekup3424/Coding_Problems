#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main(int argc, char const *argv[])
{
    int n, t;
    cin >> n >> t;
    vector<int> state(n);
    for (int i = 0; i < n; i++)
    {
        cin >> state[i];
    }

    return 0;
}
