#include <bits/stdc++.h>
using namespace std;
int main(){
  int p,c,m,err = 0;
  string name;
  try {
  cout<<"Enter physics marks:=";
  cin>>p;
  if(p<0 || p > 100){
      err = throw(p);
    }
  } catch (err) {
  
  }
}
