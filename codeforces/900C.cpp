#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main(int argc, char const *argv[])
{
    ll T;
    cin >> T;
    while (T--)
    {
        ll n;
        cin >> n;
        vector<long long> v(n);
        vector<int> max_so_far;
        for (int i = 0; i < n; i++)
        {
            /* code */
            cin >> v[i];
        }
    }
    return 0;
}
