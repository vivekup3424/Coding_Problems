#include <bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
    int t;
    while (t--)
    {
        int n;
        cin>>n;
        vector<int> A(n,0);
        vector<int> B(n,0);
        for(int i = 0; i < n; i++){
            cin>>A[i];
        }
        for(int i = 0; i < n; i++){
            cin>>B[i];
        }
        unordered_map<int,int
        int score = 0;
        int l = 0, r = *max_element(A.begin(), A.end());
        int mid = (l+r)/2;
    }
    
    return 0;
}
