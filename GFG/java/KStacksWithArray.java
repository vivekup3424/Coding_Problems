public class KStacksWithArray {
    private int[] arr; //this will server as memory
    private int[] top;
    private int[] next;
    int counter = 0;
    KStacksWithArray(int n, int k){
        arr = new int[n];
        top = new int[k];
        next = new int[k];
    }
    void add(int i, int val){
        //add val to ith stack
        if(top[i]==-1) exit
    }
}