// Weighted job sequencing problem

// Given N jobs where every job is represented by following three elements of it.
//
//     Start Time
//     Finish Time
//     Profit or Value Associated (>= 0)

#include <bits/stdc++.h>
using namespace std;

int main()
{
    int N;
    cin >> N;
    map<int, vector<int, int>> collections;
    while (N--)
    {
        int start, finish, profit;
        cin >> start >> finish >> profit;
        map
    }
}