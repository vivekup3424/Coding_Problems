// Find the union and intersection of two unsorted arrays

#include <bits/stdc++.h>
using namespace std;
// First of all sort the array, then the problem can be solved in O(nlogn) time
// I am thinking of solving this using two pointer approach

// taking sorted array as input
vector<int> union(vector<int> a, vector<int> b)
{
}