#include <bits/stdc++.h>
using namespace std;
long long countPairs(vector<int> &arr, vector<int> &brr)
{
    int n = arr.size();
    int m = brr.size();
    sort(brr.begin(), brr.end());
    long long count = 0;
}