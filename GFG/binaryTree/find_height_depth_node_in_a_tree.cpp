#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

// treenode
struct Node
{
    int data;
    Node *left;
    Node *right;
};

pair<int, int> findHeightAndDepth(Node *root, int k)
{
    int height = 0, depth = 0;
    queue<pair<Node *, int>> q;
    q.push(j)
}