import fs from "fs";

fs.readFile("./input1", { encoding: "utf-8" }, (err, data) => {
    if (err) {
        console.error(err);
    } else {
        const values = data.split("\n");
        for (let i = 0; i < values.length; i++) {
            let word1 = ""
        }
    }
});
