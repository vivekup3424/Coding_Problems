package main

func check(i, j int, e, m byte, board [][]byte, r, c int) {
	cnt := 0
	r += i
	c += j
	for r >= 0 && r < len(board) && c >= 0 && c < len(baord[0]) {

	}
}
func checkMove(board [][]byte, rMove int, cMove int, color byte) bool {

}
