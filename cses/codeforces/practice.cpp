#include <algorithm>
#include <cstdio>
#include <vector>
using namespace std;


int main(){
    frepoen("shall.in","r",stdin);
    int n;
    scanf("%d",&n);
    vector<int> shell_at_pos(3);
    for (int i = 0; i < 3; i++)
    {
        shell_at_posp[i] = i;
    }
    vector<int>counter(3);
    //number of times the shell with number i was picked

    for(int i = 0; i < n; i++){
        int a,b,g;
        scanf("%d %d %d", &a,&b,&g);
        
    }
    
}