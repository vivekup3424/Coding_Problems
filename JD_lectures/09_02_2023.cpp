
#include <bits/stdc++.h>
using namespace std;

int main()
{
}

// we require ordering of the nodes,
// Topological ordering
// It is a ordering of the node such a way in acyclic graph, if a is connected with b then the node a appears before b
// it is not neccessary to have a unique topological ordering.

// Using DP
//  path(x): is the numer of path from starting node a to x
